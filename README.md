# Array.prototype.last

### download:

```bash
npm install last --save
```

### use:

```javascript
const last = require('last');

var array = [1, 2, 3, 4, 5];

console.log(array.last);
```

*output*

```bash
5
```