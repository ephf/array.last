# Array.prototype.last

### download:

```bash
npm install arr.last --save
```

### use:

```javascript
require('arr.last');

var array = [1, 2, 3, 4, 5];

console.log(array.last);
```

*output*

```bash
5
```